

- Link to Create Product and Create Location. The name should link to a show page for that company.
- Link to delete a company.


<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th> Name </th>
      <th> Suffix </th>
      <th> Catchphrase </th>
      <th> Email </th>
      <th> Start Date </th>
      <th> Industry </th>
    </tr>

    <tbody>
      <% @companies.each do |company| %>
      <tr>
        <td><%= link_to company.name, company_path(company) %></td>
        <td><%= company.suffix %></td>
        <td><%= company.catch_phrase %></td>
        <td><%= company.email %></td>
        <td><%= company.start_date %></td>
        <td><%= company.industry %></td>
        <td><%= link_to 'Create Product', new_company_path, class: 'btn btn-info' %></td>
        <td><%= link_to 'Create Location', new_company_location_path(company), class: 'btn btn-info' %></td>

          </tr>
          <tbody>
            <% end %>
          </table>
